{ "homepage": "https://blacknrekt.github.io/FCC-Chroroplet-map/index.html",
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"  
  }
}